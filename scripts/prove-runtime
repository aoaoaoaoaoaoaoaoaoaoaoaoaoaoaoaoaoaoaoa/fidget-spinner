#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$root"

cargo test --locked --workspace --all-targets --all-features
cargo run --locked --release --package fidget-spinner-portability -- lifecycle
